/* Ridgewood v0.6.0 delta loader: upgrades the validated v0.5.1 loader. */
const response = await fetch('game-loader-v4.3.0.js?v=0.5.3-base', { cache: 'no-store' });
if (!response.ok) throw new Error(`Base game loader failed to load (${response.status}).`);
let source = await response.text();
function replaceRequired(search, replacement, label) {
  if (!source.includes(search)) throw new Error(`Voxel upgrade failed (${label}).`);
  source = source.replace(search, replacement);
}
replaceRequired("/* Ridgewood v0.5.1 loader: chat bridge + persistent cursor building. */\nconst GAME_SOURCE_URL = './multiplayer-hills-v4.1.0.js?v=0.5.1';", "/* Ridgewood v0.6.0 loader: persistent voxel terrain, collision, and cursor building. */\nconst GAME_SOURCE_URL = './multiplayer-hills-v4.1.0.js?v=0.6.0';", 'patch 1');
replaceRequired("stylesheet.href = 'world-v0.5.0.css?v=0.5.1';", "stylesheet.href = 'world-v0.5.0.css?v=0.6.0';", 'patch 2');
replaceRequired("const BLOCK_NAME_TO_TYPE = { grass: BLOCK_GRASS, dirt: BLOCK_DIRT, stone: BLOCK_STONE };", "const BLOCK_NAME_TO_TYPE = { grass: BLOCK_GRASS, dirt: BLOCK_DIRT, stone: BLOCK_STONE };\nconst WORLD_FLOOR = 0;\nconst PLAYER_RADIUS = 0.34;\nconst PLAYER_HEIGHT = 1.72;\nconst PLAYER_COLLISION_SAMPLES = [\n  [0, 0],\n  [PLAYER_RADIUS, 0], [-PLAYER_RADIUS, 0], [0, PLAYER_RADIUS], [0, -PLAYER_RADIUS],\n  [PLAYER_RADIUS * 0.72, PLAYER_RADIUS * 0.72],\n  [PLAYER_RADIUS * 0.72, -PLAYER_RADIUS * 0.72],\n  [-PLAYER_RADIUS * 0.72, PLAYER_RADIUS * 0.72],\n  [-PLAYER_RADIUS * 0.72, -PLAYER_RADIUS * 0.72]\n];\n", 'patch 3');
replaceRequired("function appendWorldOverlay(instances, cx, cz) {\n  const record = worldState.chunks.get(worldChunkKey(cx, cz));\n  if (!record) return;\n  for (const [key, value] of Object.entries(record.voxelData || {})) {\n    const parts = key.split(':').map(Number);\n    const localX = parts[0], y = parts[1], localZ = parts[2];\n    if (![localX, y, localZ].every(Number.isFinite) || value?.action !== 'place') continue;\n    const type = BLOCK_NAME_TO_TYPE[value?.block?.type] ?? BLOCK_STONE;\n    instances.push(cx * CHUNK_SIZE + localX + 0.5, y + 0.5, cz * CHUNK_SIZE + localZ + 0.5, type);\n  }\n}\n\nfunction overlayColumnHeight(worldCellX, worldCellZ, baseHeight) {\n  const cx = Math.floor(worldCellX / CHUNK_SIZE);\n  const cz = Math.floor(worldCellZ / CHUNK_SIZE);\n  const localX = positiveLocal(worldCellX);\n  const localZ = positiveLocal(worldCellZ);\n  const record = worldState.chunks.get(worldChunkKey(cx, cz));\n  let height = baseHeight;\n  for (const [key, value] of Object.entries(record?.voxelData || {})) {\n    const parts = key.split(':').map(Number);\n    if (parts[0] === localX && parts[2] === localZ && value?.action === 'place') {\n      height = Math.max(height, parts[1] + 1);\n    }\n  }\n  return height;\n}\n\nfunction baseTerrainHeightForBuild(worldCellX, worldCellZ) {\n  let height = getBaseTerrainHeight(worldCellX, worldCellZ);\n  const distance = Math.hypot(worldCellX, worldCellZ);\n  if (distance < 7) {\n    const centerHeight = getBaseTerrainHeight(0, 0);\n    const blend = smoothstep(2.0, 7.0, distance);\n    height = Math.round(mix(centerHeight, height, blend));\n  }\n  return clamp(height, MIN_HEIGHT, MAX_HEIGHT);\n}\n\nfunction placedVoxelAt(worldX, y, worldZ) {\n  const chunkX = Math.floor(worldX / CHUNK_SIZE);\n  const chunkZ = Math.floor(worldZ / CHUNK_SIZE);\n  const localX = positiveLocal(worldX);\n  const localZ = positiveLocal(worldZ);\n  const record = worldState.chunks.get(worldChunkKey(chunkX, chunkZ));\n  const value = record?.voxelData?.[String(localX) + ':' + String(y) + ':' + String(localZ)];\n  return value?.action === 'place' ? { kind: 'placed', value, record, chunkX, chunkZ, localX, localZ } : null;\n}\n\nfunction occupiedVoxelAt(worldX, y, worldZ) {\n  const placed = placedVoxelAt(worldX, y, worldZ);\n  if (placed) return placed;\n  if (y < baseTerrainHeightForBuild(worldX, worldZ)) return { kind: 'terrain' };\n  return null;\n}\n\n", "function worldVoxelEntry(worldX, y, worldZ) {\n  const chunkX = Math.floor(worldX / CHUNK_SIZE);\n  const chunkZ = Math.floor(worldZ / CHUNK_SIZE);\n  const localX = positiveLocal(worldX);\n  const localZ = positiveLocal(worldZ);\n  const record = worldState.chunks.get(worldChunkKey(chunkX, chunkZ));\n  const key = String(localX) + ':' + String(y) + ':' + String(localZ);\n  return { record, value: record?.voxelData?.[key], chunkX, chunkZ, localX, localZ, key };\n}\n\nfunction baseTerrainHeightForBuild(worldCellX, worldCellZ) {\n  let height = getBaseTerrainHeight(worldCellX, worldCellZ);\n  const distance = Math.hypot(worldCellX, worldCellZ);\n  if (distance < 7) {\n    const centerHeight = getBaseTerrainHeight(0, 0);\n    const blend = smoothstep(2.0, 7.0, distance);\n    height = Math.round(mix(centerHeight, height, blend));\n  }\n  return clamp(height, MIN_HEIGHT, MAX_HEIGHT);\n}\n\nfunction baseVoxelType(worldX, y, worldZ, height = baseTerrainHeightForBuild(worldX, worldZ)) {\n  if (y === height - 1) return getSurfaceType(worldX, worldZ, height);\n  return height - 1 - y <= 2 ? BLOCK_DIRT : BLOCK_STONE;\n}\n\nfunction occupiedVoxelAt(worldX, y, worldZ) {\n  if (![worldX, y, worldZ].every(Number.isFinite)) return null;\n  const entry = worldVoxelEntry(worldX, y, worldZ);\n  if (entry.value?.action === 'remove') return null;\n  if (entry.value?.action === 'place') {\n    return {\n      kind: 'placed',\n      type: BLOCK_NAME_TO_TYPE[entry.value?.block?.type] ?? BLOCK_STONE,\n      ...entry\n    };\n  }\n  const height = baseTerrainHeightForBuild(worldX, worldZ);\n  if (y >= WORLD_FLOOR && y < height) {\n    return { kind: y === WORLD_FLOOR ? 'bedrock' : 'terrain', type: baseVoxelType(worldX, y, worldZ, height), ...entry };\n  }\n  return null;\n}\n\nfunction reconcileWorldVoxels(instances, cx, cz) {\n  const cells = new Map();\n  for (let index = 0; index < instances.length; index += 4) {\n    const worldX = Math.floor(instances[index]);\n    const y = Math.floor(instances[index + 1]);\n    const worldZ = Math.floor(instances[index + 2]);\n    cells.set(worldX + ':' + y + ':' + worldZ, [instances[index], instances[index + 1], instances[index + 2], instances[index + 3]]);\n  }\n\n  const startX = cx * CHUNK_SIZE;\n  const startZ = cz * CHUNK_SIZE;\n  for (let localZ = 0; localZ < CHUNK_SIZE; localZ += 1) {\n    for (let localX = 0; localX < CHUNK_SIZE; localX += 1) {\n      const worldX = startX + localX;\n      const worldZ = startZ + localZ;\n      const height = baseTerrainHeightForBuild(worldX, worldZ);\n      for (let y = WORLD_FLOOR; y < height; y += 1) {\n        const key = worldX + ':' + y + ':' + worldZ;\n        const occupied = occupiedVoxelAt(worldX, y, worldZ);\n        if (!occupied) cells.delete(key);\n        else cells.set(key, [worldX + 0.5, y + 0.5, worldZ + 0.5, occupied.type]);\n      }\n    }\n  }\n\n  const record = worldState.chunks.get(worldChunkKey(cx, cz));\n  for (const [key, value] of Object.entries(record?.voxelData || {})) {\n    const [localX, y, localZ] = key.split(':').map(Number);\n    if (![localX, y, localZ].every(Number.isFinite)) continue;\n    const worldX = startX + localX;\n    const worldZ = startZ + localZ;\n    const worldKey = worldX + ':' + y + ':' + worldZ;\n    if (value?.action === 'remove') cells.delete(worldKey);\n    if (value?.action === 'place') {\n      cells.set(worldKey, [worldX + 0.5, y + 0.5, worldZ + 0.5, BLOCK_NAME_TO_TYPE[value?.block?.type] ?? BLOCK_STONE]);\n    }\n  }\n\n  instances.length = 0;\n  for (const instance of cells.values()) instances.push(...instance);\n}\n\nfunction overlayColumnHeight(worldCellX, worldCellZ, baseHeight) {\n  let highestCandidate = Math.max(baseHeight, WORLD_FLOOR + 1);\n  const entry = worldVoxelEntry(worldCellX, 0, worldCellZ);\n  for (const [key, value] of Object.entries(entry.record?.voxelData || {})) {\n    const [localX, y, localZ] = key.split(':').map(Number);\n    if (localX === entry.localX && localZ === entry.localZ && value?.action === 'place') {\n      highestCandidate = Math.max(highestCandidate, y + 1);\n    }\n  }\n  for (let y = Math.ceil(highestCandidate) - 1; y >= WORLD_FLOOR; y -= 1) {\n    if (occupiedVoxelAt(worldCellX, y, worldCellZ)) return y + 1;\n  }\n  return WORLD_FLOOR + 1;\n}\n\nfunction supportHeightAt(x, z, referenceY) {\n  const worldX = Math.floor(x);\n  const worldZ = Math.floor(z);\n  const maximumTop = Math.floor(Number(referenceY) + MAX_STEP_HEIGHT + 0.001);\n  for (let y = maximumTop - 1; y >= WORLD_FLOOR; y -= 1) {\n    if (occupiedVoxelAt(worldX, y, worldZ)) return y + 1;\n  }\n  return WORLD_FLOOR + 1;\n}\n\nfunction canOccupyPlayerAt(x, z, groundY) {\n  const minY = Math.floor(groundY + 0.04);\n  const maxY = Math.floor(groundY + PLAYER_HEIGHT - 0.04);\n  for (const [offsetX, offsetZ] of PLAYER_COLLISION_SAMPLES) {\n    const worldX = Math.floor(x + offsetX);\n    const worldZ = Math.floor(z + offsetZ);\n    for (let y = minY; y <= maxY; y += 1) {\n      if (occupiedVoxelAt(worldX, y, worldZ)) return false;\n    }\n  }\n  return true;\n}\n\nfunction resolvePlayerGround(x, z, referenceY = player.groundY || player.y) {\n  return supportHeightAt(x, z, Number.isFinite(referenceY) ? referenceY : getTerrainHeight(x, z));\n}\n\nfunction voxelIntersectsPlayer(worldX, y, worldZ) {\n  const voxelMinX = worldX;\n  const voxelMaxX = worldX + 1;\n  const voxelMinZ = worldZ;\n  const voxelMaxZ = worldZ + 1;\n  const playerMinX = player.x - PLAYER_RADIUS;\n  const playerMaxX = player.x + PLAYER_RADIUS;\n  const playerMinZ = player.z - PLAYER_RADIUS;\n  const playerMaxZ = player.z + PLAYER_RADIUS;\n  const playerMinY = player.y + 0.02;\n  const playerMaxY = player.y + PLAYER_HEIGHT - 0.02;\n  return voxelMaxX > playerMinX && voxelMinX < playerMaxX\n    && voxelMaxZ > playerMinZ && voxelMinZ < playerMaxZ\n    && y + 1 > playerMinY && y < playerMaxY;\n}\n\n", 'patch 4');
replaceRequired("function targetFromPointer() {\n  if (!worldState.pointerInside || !network.ready || window.__ridgewoodMenuOpen) return null;\n  const result = raycastWorld(worldState.pointerX, worldState.pointerY);\n  if (!result) return null;\n  const cell = worldState.buildMode === 'delete'\n    ? (result.hit?.occupied?.kind === 'placed' ? result.hit : null)\n    : result.place;\n  if (!cell) return null;\n  const chunkX = Math.floor(cell.worldX / CHUNK_SIZE);\n  const chunkZ = Math.floor(cell.worldZ / CHUNK_SIZE);\n  const localX = positiveLocal(cell.worldX);\n  const localZ = positiveLocal(cell.worldZ);\n  const record = worldState.chunks.get(worldChunkKey(chunkX, chunkZ));\n  const distance = Math.hypot(\n    cell.worldX + 0.5 - player.x,\n    cell.y + 0.5 - player.y,\n    cell.worldZ + 0.5 - player.z\n  );\n  const owns = worldOwnsChunk(record);\n  const valid = owns && distance <= 8.0 && cell.y >= -64 && cell.y <= 96;\n  return {\n    ...cell, chunkX, chunkZ, localX, localZ, record, distance, valid,\n    reason: !owns ? 'You can only build inside one of your claimed chunks.'\n      : distance > 8.0 ? 'Move closer to that block.' : 'That location cannot be edited.'\n  };\n}\n\n", "function targetFromPointer() {\n  if (!worldState.pointerInside || !network.ready || window.__ridgewoodMenuOpen) return null;\n  const result = raycastWorld(worldState.pointerX, worldState.pointerY);\n  if (!result) return null;\n  const cell = worldState.buildMode === 'delete'\n    ? (result.hit?.occupied ? result.hit : null)\n    : result.place;\n  if (!cell) return null;\n  const chunkX = Math.floor(cell.worldX / CHUNK_SIZE);\n  const chunkZ = Math.floor(cell.worldZ / CHUNK_SIZE);\n  const localX = positiveLocal(cell.worldX);\n  const localZ = positiveLocal(cell.worldZ);\n  const record = worldState.chunks.get(worldChunkKey(chunkX, chunkZ));\n  const distance = Math.hypot(\n    cell.worldX + 0.5 - player.x,\n    cell.y + 0.5 - player.y,\n    cell.worldZ + 0.5 - player.z\n  );\n  const owns = worldOwnsChunk(record);\n  const isDelete = worldState.buildMode === 'delete';\n  const bedrock = isDelete && cell.y <= WORLD_FLOOR;\n  const intersectsPlayer = !isDelete && voxelIntersectsPlayer(cell.worldX, cell.y, cell.worldZ);\n  const valid = owns && distance <= 8.0 && cell.y >= WORLD_FLOOR && cell.y <= 96 && !bedrock && !intersectsPlayer;\n  return {\n    ...cell, chunkX, chunkZ, localX, localZ, record, distance, valid,\n    reason: !owns ? 'You can only edit blocks inside one of your claimed chunks.'\n      : distance > 8.0 ? 'Move closer to that block.'\n        : bedrock ? 'The bottom bedrock layer cannot be removed.'\n          : intersectsPlayer ? 'You cannot place a block inside your character.'\n            : 'That location cannot be edited.'\n  };\n}\n\n", 'patch 5');
replaceRequired("worldFeedback('World saved.', 'ok');", "worldFeedback(message.edit?.action === 'remove' ? 'Block removed and world saved.' : 'Block placed and world saved.', 'ok');", 'patch 6');
replaceRequired("const labels = { not_owner: 'That chunk belongs to another player.', chunk_not_claimed: 'Claim this chunk before building.', too_far: 'Move closer to the build location.', rate_limited: 'You are building too quickly.' };", "const labels = { not_owner: 'That chunk belongs to another player.', chunk_not_claimed: 'Claim this chunk before building.', too_far: 'Move closer to the build location.', rate_limited: 'You are building too quickly.', bedrock: 'The bottom bedrock layer cannot be removed.', intersects_player: 'You cannot place a block inside your character.' };", 'patch 7');
replaceRequired("player.groundY = getTerrainHeight(player.x, player.z); player.y = player.groundY;", "player.groundY = resolvePlayerGround(player.x, player.z, player.y); player.y = player.groundY;", 'patch 8');
replaceRequired("  source = replaceRequired(\n    source,\n    `  window.addEventListener('keydown', (event) => {\n    if (movementKeys.has(event.code)) {`,", "  source = replaceRequired(\n    source,\n    `function movePlayer(dx, dz) {\n  const currentHeight = getTerrainHeight(player.x, player.z);\n  const targetX = player.x + dx;\n  const targetZ = player.z + dz;\n  const targetHeight = getTerrainHeight(targetX, targetZ);\n\n  if (Math.abs(targetHeight - currentHeight) <= MAX_STEP_HEIGHT) {\n    player.x = targetX;\n    player.z = targetZ;\n    return;\n  }\n\n  // Slide along a blocked ledge rather than stopping completely.\n  const xHeight = getTerrainHeight(targetX, player.z);\n  if (Math.abs(xHeight - currentHeight) <= MAX_STEP_HEIGHT) player.x = targetX;\n  const zHeight = getTerrainHeight(player.x, targetZ);\n  if (Math.abs(zHeight - getTerrainHeight(player.x, player.z)) <= MAX_STEP_HEIGHT) player.z = targetZ;\n}`,\n    `function movePlayer(dx, dz) {\n  const currentGround = resolvePlayerGround(player.x, player.z, player.groundY || player.y);\n\n  const attempt = (targetX, targetZ) => {\n    const targetGround = resolvePlayerGround(targetX, targetZ, currentGround);\n    if (Math.abs(targetGround - currentGround) > MAX_STEP_HEIGHT) return false;\n    if (!canOccupyPlayerAt(targetX, targetZ, targetGround)) return false;\n    player.x = targetX;\n    player.z = targetZ;\n    player.groundY = targetGround;\n    return true;\n  };\n\n  if (attempt(player.x + dx, player.z + dz)) return;\n  const originX = player.x;\n  const originZ = player.z;\n  if (!attempt(originX + dx, originZ)) attempt(originX, originZ + dz);\n}`,\n    'voxel player collision'\n  );\n\n  source = replaceRequired(\n    source,\n    `  player.groundY = getTerrainHeight(player.x, player.z);\n  player.y = damp(player.y, player.groundY, 20, dt);`,\n    `  player.groundY = resolvePlayerGround(player.x, player.z, player.groundY || player.y);\n  player.y = damp(player.y, player.groundY, 20, dt);`,\n    'voxel ground resolution'\n  );\n\n  source = replaceRequired(\n    source,\n    `  window.addEventListener('keydown', (event) => {\n    if (movementKeys.has(event.code)) {`,", 'patch 9');
replaceRequired("`  appendWorldOverlay(instances, cx, cz);\n  const instanceData = new Float32Array(instances);`", "`  reconcileWorldVoxels(instances, cx, cz);\n  const instanceData = new Float32Array(instances);`", 'patch 10');
replaceRequired("source = source.replaceAll('MULTIPLAYER HILLS v4.1.0', 'RIDGEWOOD v0.5.1 ALPHA');", "source = source.replaceAll('MULTIPLAYER HILLS v4.1.0', 'RIDGEWOOD v0.6.0 ALPHA');", 'patch 11');
replaceRequired("source = source.replaceAll('multiplayer-hills-4-1-0', 'ridgewood-0-5-1');", "source = source.replaceAll('multiplayer-hills-4-1-0', 'ridgewood-0-6-0');", 'patch 12');
const blobUrl = URL.createObjectURL(new Blob([source], { type: 'text/javascript' }));
try { await import(blobUrl); } finally { URL.revokeObjectURL(blobUrl); }
